project('dining_philosophers', 'c',
  version : '0.1',
  default_options : ['warning_level=3'])

exe = executable('dining_philosophers', 'dining_philosophers.c',
  install : true)

test('basic', exe)
